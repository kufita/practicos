function suma(a:number, b: number):number {
  return a+b  
}

suma(2,3);

const munlti=(a:number,b:number):number=> a + b;
let firstCheck = true;
// condicion ? "true":"falso"
let accessc= firstCheck ? "Acceso permitido": "Acceso denegado"
console.log(accessc)